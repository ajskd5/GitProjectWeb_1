<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.mapper.reply-mapper">
	<!-- 목록 type, bno 출력-->
	<!-- 댓글 추가 => 게시판 제목에 댓글 수 +1 -->
	<update id="countIncrement" parameterType="ReplyVO">
		UPDATE ${table_name} SET
		rcount = rcount + 1
		WHERE no=#{bno}
	</update>
	<select id="replyListData" resultType="ReplyVO" parameterType="ReplyVO">
		SELECT /*+ INDEX_DESC(poject_reply pr_no_pk)*/ no, bno, type, id, name, msg, TO_CHAR(regdate, 'YYYY-MM-DD HH24:MI:ss') as dbday
		FROM project_reply
		WHERE bno=#{bno} AND type=#{type}
	</select>
	
	<!-- 추가 -->
	<insert id="replyInsertData" parameterType="ReplyVO">
		<!-- sequence 자동증가번호 	order="before" => 얘를 먼저 수행하고 insert 나중에 수행 -->
		<selectKey keyProperty="no" resultType="int" order="BEFORE">
			SELECT NVL(MAX(no)+1, 1) as no FROM project_reply
		</selectKey>
		INSERT INTO project_reply
		VALUES(#{no}, #{bno}, #{type}, #{id}, #{name}, #{msg}, SYSDATE)
	</insert>

	<!-- 삭제 -->
	<delete id="replyDelete" parameterType="int">
		DELETE FROM project_reply WHERE no=#{no}
	</delete>
	
	<!-- 수정 -->
	<update id="replyUpdate" parameterType="ReplyVO">
		UPDATE project_reply SET
		msg = #{msg}
		WHERE no = #{no}
	</update>
</mapper>